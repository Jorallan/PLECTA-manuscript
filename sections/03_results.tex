\section{Results}
\label{sec:results}

\subsection{Held-out synthetic reconstruction}
\label{sec:res-density}

PLECTA completed all \PlectaHeldoutSceneCount{} held-out scenes with
\PlectaHeldoutFailureCount{} failures. Mean common-fragment pairwise
\Fone{} was \PlectaHeldoutFOneMean{} (density-stratified 95\% bootstrap CI
[\PlectaHeldoutFOneCILow{}, \PlectaHeldoutFOneCIHigh{}]);
Table~\ref{tab:plecta-heldout} gives precision, recall, reference-instance
recovery, adjusted Rand index and both variation-of-information components,
overall and by density. These scenes used unseen seeds from the same
procedural generator as development data, so this is same-generator
generalisation and not distribution transfer
(Section~\ref{sec:limitations}).

\begin{table}[htbp]
    \centering
    \caption{Held-out reconstruction scores overall and by target areal
    coverage. VI denotes variation of information, for which lower is better;
    higher is better for the other metrics.}
    \label{tab:plecta-heldout}
    \small
    \input{results/plecta_heldout_table}
\end{table}

Performance decreased with scene density: mean \Fone{} fell from
\PlectaDensityTwoZeroFOneMean{}
[\PlectaDensityTwoZeroFOneCILow{}, \PlectaDensityTwoZeroFOneCIHigh{}] at 20\%
coverage to \PlectaDensitySixZeroFOneMean{}
[\PlectaDensitySixZeroFOneCILow{}, \PlectaDensitySixZeroFOneCIHigh{}] at 60\%,
monotonically through the intermediate strata. The same decline appears on the paired
robustness set (Figure~\ref{fig:density}): the two curves separate at low
coverage and meet at 50--60\%, degradation costing least exactly where the score
is lowest, because there the tangles rather than the gaps set the difficulty.
Per-scene runtime had mean \PlectaHeldoutRuntimeMean{}~s, median
\PlectaHeldoutRuntimeMedian{}~s, 90th percentile
\PlectaHeldoutRuntimePNinety{}~s, and maximum \PlectaHeldoutRuntimeMaximum{}~s;
these timings are hardware-specific.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\linewidth]{figures/fig_plecta_density.pdf}
    \caption{Coverage response on the \PlectaRobustnessSceneCount{} paired
    scenes, ten per coverage level, each scored twice: from the degraded 1\,px
    axis mask that the paper evaluates, and from the generator's undegraded
    axis. Lines are means of per-scene common-fragment $F_1$; bands are 95\,\%
    percentile bootstrap intervals for the mean, resampled within coverage
    stratum (20\,000 replicates, seed 20260811). Target areal coverage is the
    generator setting used to dial in centreline density.}
    \label{fig:density}
\end{figure}

\subsection{Which density axis matters}
\label{sec:res-factorial}

Coverage is the fraction of the frame filled by the rendered bundles, so it
rises both when filaments are added and when the same filaments are drawn
thicker, and only the first changes the grouping problem. A factorial separates
them: twelve geometries, three centreline length densities crossed with four
seeds, were each rendered at bundle widths of 6, 11 and 16 px. Within one geometry the input axis mask and the overlap-aware
centreline reference are byte-identical across the three widths, verified by
SHA-256, while the union of the thick supports changes: areal coverage varies by
up to a factor of \PlectaFactorialMaxCoverageRatio{}. Any movement in the score
across those three renderings would mean that the method or the metric reads the
silhouette.

Across all \PlectaFactorialGeometryCount{} geometries the within-geometry
\Fone{} range was \PlectaFactorialMaxFOneRange{}, that is, exactly zero in
\PlectaFactorialZeroRangeCount{} of \PlectaFactorialGeometryCount{}
(Table~\ref{tab:plecta-factorial}). Areal coverage therefore cannot causally
affect the grouping score; PLECTA reads the axis mask, and the metric scores
centreline fragments, so neither sees the width the bundles were drawn at.
Centreline length density does move it, mean \Fone{} falling from
\PlectaFactorialLowFOne{} to \PlectaFactorialHighFOne{} across the three
densities of Table~\ref{tab:plecta-factorial}. The coverage bands of the three length
densities also overlap, so coverage does not identify a difficulty level on its
own. Density strata reported by target areal coverage should therefore be read
as a proxy for the length density accompanying them under this generator.

\begin{table}[htbp]
    \centering
    \caption{Bundle width against centreline length density,
    \PlectaFactorialSceneCount{} development scenes. Within each row the three
    bundle widths share one byte-identical axis mask and one reference, so the
    spread in areal coverage carries no score difference. Length density is
    centreline pixels per image pixel.}
    \label{tab:plecta-factorial}
    \small
    \input{results/plecta_factorial_table}
\end{table}

\subsection{Comparison with prior linkers}
\label{sec:res-comparators}

One baseline and two published methods were run on identical input masks with
one scorer. The baseline is a greedy minimum-turning-angle continuation rule,
and it is ours: not a reimplementation of any particular paper but of the
pairing principle several of them share, reported as the transparent floor the
claim of Section~\ref{sec:intro} is made against and never as an external
result. The published methods are DNAi \citep{playout2026dnai} and GraFT
\citep{osterlund2026graft}, each executed from its own released source. All were
scored through the same conversion and metric code as PLECTA, and that path was
gated on parity: before any comparator number was believed it had to reproduce
PLECTA's stored \Fone{}, adjusted Rand index and both variation-of-information
components, because a faulty conversion yields plausible rather than obviously
wrong scores. Each comparator received a parameter translation selected on the
same \PlectaDevelopmentSceneCount{} development scenes PLECTA was configured
on, and every adaptation below was checked and found to favour the comparator.

\textbf{Greedy continuation.} The contribution claimed in
Section~\ref{sec:intro} is that a junction is better solved as one exact
matching, with an explicit priced option to leave an arm unmatched, than by
pairing endpoints greedily in priority order, the rule used by SIFNE, by GraFT's
path cover, and by the continuity baseline of Wegmayr et al.\
\citep{zhang2017sifne,osterlund2026graft,wegmayr2020microplastic}. The
comparator therefore skeletonises the same mask, prunes spurs, splits the
skeleton into branches, and greedily accepts the lowest-turning-angle pairing
between branch ends within a distance and a turning-angle gate, both gates fixed
by a grid sweep for best mean development \Fone{}. On
\PlectaGreedySceneCount{} scenes generated after both configurations were fixed,
PLECTA scored \PlectaGreedyPlectaFOne{} against
\PlectaGreedySweptBaselineFOne{} and was better in
\PlectaGreedySweptWinCount{} of them; Table~\ref{tab:plecta-comparators} gives
that difference and every other, the field-standard measures ranking the two as
the in-house one does. On clean axes the
difference was \PlectaGreedySweptCleanDeltaFOne{}
[\PlectaGreedySweptCleanDeltaCILow{}, \PlectaGreedySweptCleanDeltaCIHigh{}], so
it is not an artefact of mask degradation.

\begin{table}[htbp]
    \centering
    \caption{All four methods on the degraded masks, identical input masks and
    one scorer. $n=\PlectaGreedySceneCount{}$ scenes and
    \PlectaJunctionCount{} reference crossings, except GraFT, over the
    \PlectaGraFTSceneCount{} it completed. Variation of information is in bits
    and lower is better; higher is better for the rest. The last two rows are
    defined in Section~\ref{sec:res-overlap}. The final column is the paired
    difference against the greedy continuation, density-stratified 95\%
    bootstrap; the other paired differences are in the text.}
    \label{tab:plecta-comparators}
    \small
    \input{results/plecta_comparator_table}
\end{table}

\textbf{Exact against greedy matching, matcher only.} That baseline replaces
the whole linker; a second arm isolates the claim, keeping the graph, cost,
admissibility gate and round schedule and replacing only the per-junction
maximum-weight matching by greedy cheapest-first acceptance. Exactness was
worth \PlectaMatcherDeltaFOne{} \Fone{} [\PlectaMatcherDeltaCILow{},
\PlectaMatcherDeltaCIHigh{}] over \PlectaMatcherSceneCount{} development
scenes, on \PlectaMatcherWinCount{} of \PlectaMatcherSceneCount{} of them, and
\PlectaMatcherCleanDeltaFOne{} [\PlectaMatcherCleanDeltaCILow{},
\PlectaMatcherCleanDeltaCIHigh{}] on clean axes; the failure is division, not
merging, with variation-of-information split rising
\PlectaMatcherExactVISplit{} to \PlectaMatcherGreedyVISplit{} at unchanged
merge, \PlectaMatcherInstanceExcess{}\,\% more instances emitted and crossings
resolved exactly falling \PlectaMatcherExactJunctionRate{} to
\PlectaMatcherGreedyJunctionRate{}. The relative loss grows monotonically with
degree, \PlectaMatcherDegreeThreeRelative{}\,\% at degree three to
\PlectaMatcherDegreeSixRelative{}\,\% at degree six, but in absolute terms
peaks at degree four (\PlectaMatcherDegreeFourAbsolute{}), where most crossings
lie; and the greedy arm runs at parameters selected with the exact matcher in
the loop, a bias in that matcher's favour.


\textbf{DNAi.} DNAi post-processes a binary segmentation by the sequence
described in Section~\ref{sec:intro}; its segmentation network was bypassed and
only that stage used, so grouping is not confounded with segmentation quality.
At its released defaults it scored \Fone{}=\PlectaDNAiShippedFOne{} on the
degraded masks, which is not a fair measure of it: its single-linkage clustering
of junction pixels at 10~px chains together crossings that lie closer than that,
and at 60\% coverage the largest cluster spans 89~px and is replaced by one
small erasure disk, leaving the crossings along it unseparated and the whole
tangle a single instance. Reducing that distance to 2~px on development scenes
raised development \Fone{} from 0.211 to 0.378 and cut variation-of-information
merge from 2.07 to 0.66, confirming the mechanism; dilating the input to a
thicker mask was also tested across 41 development configurations and made DNAi
worse at every setting, so it was not used. At its best development-selected
configuration DNAi scored \PlectaDNAiFOne{}, a paired difference of
\PlectaDNAiDeltaFOne{} [\PlectaDNAiDeltaCILow{}, \PlectaDNAiDeltaCIHigh{}] over
\PlectaDNAiSceneCount{} scenes, and \PlectaDNAiCleanDeltaFOne{}
[\PlectaDNAiCleanDeltaCILow{}, \PlectaDNAiCleanDeltaCIHigh{}] on clean axes,
the gap widening monotonically with density (Figure~\ref{fig:comparators}).
Both methods must be read against the one-instance-per-connected-component floor
at their own density, since that floor is itself strongly density-dependent and
a pooled value would flatter any method at 60\% coverage.

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/fig_comparators.pdf}
  \caption{The comparison of Table~\ref{tab:plecta-comparators} by coverage
    stratum. \textbf{(a)}~degraded and \textbf{(b)}~clean input mask: mean
    common-fragment \Fone{}, $n=10$ scenes per point
    except where labelled otherwise; plain means, no interval is drawn and none
    is claimed. The aggregate paired differences and their intervals are in the
    table. GraFT exceeded its per-scene wall-clock budget on 3 of the 50
    degraded scenes and 1 of the clean ones, the hardest of the densest
    stratum, so its curve is optimistic.}
  \label{fig:comparators}
\end{figure}

That margin is a limitation of domain and density, not a general ranking. DNAi
is built for DNA-fibre micrographs, which are sparse: its own bundled test image
contains five junction clusters, whereas our scenes contain between 23 and 183,
and where the network is sparse and the axis clean it recovers
\PlectaDNAiCleanSparseFOne{} at 20\% coverage. Two structural differences
explain the rest. It commits to a pairing at every junction of degree two to
four with no cost threshold, so it cannot decline a continuation the way a
priced unmatched option can; and it resolves only junctions of degree at most
four, measurably so, resolving \PlectaJunctionDNAiDegreeFivePlus{} of the
crossings above that degree. Those are about a tenth of the crossings but carry
\PlectaEvidenceHighDegree{}\,\% of same-filament fragment pairs on the degraded
masks, one such junction severing every pair that spans it.

Restricting the score to the evidence a degree-limited, gap-blind method can
reach separates capability from judgement. On clean axes the difference falls
from \PlectaDNAiCleanDeltaFOne{} to \PlectaDNAiRestrictedCleanDeltaFOne{}
[\PlectaDNAiRestrictedCleanDeltaCILow{},
\PlectaDNAiRestrictedCleanDeltaCIHigh{}], still favouring PLECTA in
\PlectaDNAiRestrictedCleanWinCount{} of \PlectaDNAiCleanSceneCount{} scenes and
at every coverage level. DNAi gains far more from the restriction than PLECTA
does, which is what shows the restriction genuinely removes its handicap. So
roughly \PlectaDNAiCoverageShareClean{}\,\% of the margin is coverage of cases
the comparator cannot attempt and \PlectaDNAiDecisionShareClean{}\,\% is the
outcome of decisions both methods make. Consistent with that, after its
clustering is repaired DNAi's remaining errors are dominated by division, a
third of them at junctions of degree four, which it does attempt.

\textbf{GraFT.} GraFT builds a graph on a skeleton, derives an angle threshold
per component, and traces filaments by constrained depth-first search followed
by a greedy longest-first path cover. Its published entry point begins with a
Frangi and Otsu front end for grayscale ridge images, which has nothing to do
on a two-valued axis, so the mask was injected at its skeleton-to-graph stage,
everything downstream being its own code unchanged.
That substitution favours GraFT: run end to end on the grayscale images it
scores \PlectaGraFTNativeFOne{}, near the connectivity floor, because its
segmentation stage recovers only a third to a half of the reference skeleton.
Its minimum continuation angle was re-tuned on development scenes, gaining it a
further 0.04. GraFT scored \PlectaGraFTFOne{}, a paired difference of
\PlectaGraFTDeltaFOne{} [\PlectaGraFTDeltaCILow{}, \PlectaGraFTDeltaCIHigh{}]
favouring PLECTA in every one of the \PlectaGraFTSceneCount{} scenes both
methods completed, and \PlectaGraFTCleanDeltaFOne{}
[\PlectaGraFTCleanDeltaCILow{}, \PlectaGraFTCleanDeltaCIHigh{}] on clean axes.
Its errors are divisions rather than merges (variation of information split
\PlectaGraFTVISplit{} against merge \PlectaGraFTVIMerge{}), which follows from a
greedy cover that cannot revise an early commitment once the scene collapses
into one connected graph, as it does above 30\% coverage.
\PlectaGraFTFailed{} of the densest degraded scenes did not finish inside a
45-minute budget and are excluded from its means; they are the hardest scenes of
the set, so that exclusion flatters GraFT. Neither released method establishes a
general ranking, and Section~\ref{sec:limitations} states what these comparisons
leave untested.

\textbf{One frame that is not ours.} Every scene above comes from one generator,
which Section~\ref{sec:limitations} names as this paper's principal limitation,
so it is worth reporting that GraFT's repository bundles a single
\PlectaGraFTFrameSide{}$\times$\PlectaGraFTFrameSide{}~px synthetic frame with
per-filament ground truth, and that the ordering survives it. Scored through the
identical pipeline, PLECTA reached \Fone{}=\PlectaGraFTFramePlectaFOne{} against
\PlectaGraFTFrameGreedyFOne{} for the greedy baseline,
\PlectaGraFTFrameGraFTNativeFOne{} for GraFT run end to end on its own noisy
image and \PlectaGraFTFrameGraFTInjectedFOne{} for GraFT with the mask injected
as it is elsewhere here; the rasterisation of that reference reproduces the
vendor's own noise-free image at \PlectaGraFTFrameReferenceAgreement{}. This is
one frame with no interval and it is not a benchmark. Its instructive detail is
that GraFT resolved \PlectaGraFTFrameGraFTInjectedSolved{} of the frame's
\PlectaGraFTFrameCrossingCount{} crossings exactly while scoring
\PlectaGraFTFrameGraFTInjectedFOne{}, where PLECTA resolved
\PlectaGraFTFramePlectaSolved{}: crossing accuracy and pairwise agreement
answer to different failures, which is why Section~\ref{sec:res-overlap} reports
both.

\textbf{A ladder on GraFT's own ground.} One frame is an anecdote, so the same
question was put to GraFT's own generator, run unmodified at its published
settings, over \PlectaRegimeSceneCount{} scenes in
\PlectaRegimeStratumCount{} density strata
(Figure~\ref{fig:regime}). The ladder spans
\PlectaRegimeDensityLow{} to \PlectaRegimeDensityHigh{} centreline pixels per
image pixel: GraFT's published comparison reaches
\PlectaRegimeDensityPublished{}, so its whole range is the lower half of this
one and this paper's own scenes occupy the upper half. On the endpoint used
throughout this paper PLECTA leads at every density, from
\PlectaRegimeLowPlectaFOne{} against \PlectaRegimeLowGraFTFOne{} and
\PlectaRegimeLowDNAiFOne{} at the sparsest, to
\PlectaRegimeHighPlectaFOne{} against \PlectaRegimeHighGraFTFOne{} and
\PlectaRegimeHighDNAiFOne{} at the densest; the margin widens with density and
nothing crosses over, including inside GraFT's published range. GraFT was also
run end to end on the noisy images with its own front end, which on this data
is inside its design envelope rather than degenerate as it is on ours; that arm
scored \PlectaRegimeLowGraFTNativeFOne{} and
\PlectaRegimeHighGraFTNativeFOne{} at the two ends, below the injected arm
almost everywhere, so injecting the mask continues to favour it.

The same predictions score differently under GraFT's own measure, and the
disagreement is worth stating rather than resolving. Filament matched coverage
asks whether each reference filament was matched one-to-one to some detection.
At the densest stratum it reads \PlectaRegimeHighGraFTMatched{} for GraFT,
\PlectaRegimeHighDNAiMatched{} for DNAi and \PlectaRegimeHighPlectaMatched{}
for PLECTA --- the reverse order. Because the matching is one-to-one, a method
emitting fewer objects than the scene contains is capped by that ratio, and
PLECTA emits \PlectaRegimeHighPlectaEmitted{} objects per reference filament
against \PlectaRegimeHighGraFTEmitted{} for GraFT: PLECTA's coverage figure is
its emission ratio to three decimals. The measure is not wrong. It asks whether
each filament was found, and a method that severs every filament at its
crossings still offers a distinct detection for each one; it cannot ask whether
the pieces were then put together correctly, which is what the pairwise score
asks and what this paper is about. Both are reported here for that reason.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{figures/fig_graft_regime.pdf}
  \caption{Three methods on GraFT's own generator, at its published settings,
    over \PlectaRegimeSceneCount{} scenes; means of five scenes per stratum,
    no interval drawn and none claimed. Dashed line: the density at which
    GraFT's published comparison ends. \textbf{(a)}~the endpoint used
    throughout this paper. \textbf{(b)}~GraFT's own filament matched coverage,
    on the identical predictions, which reverses the order at high density.
    Its vertical axis is truncated to the range the values occupy; that of
    \textbf{(a)} is not.}
  \label{fig:regime}
\end{figure}

Per-scene cost was measured warm, single-threaded and pinned to one core, timing
each method\'s own computation only (Table~\ref{tab:plecta-comparators};
\RuntimeGraftCovSixtyCensored{} GraFT scenes exceeded \RuntimeBudgetSeconds{}~s
and are censored). These are independently optimised codebases, so absolute
times measure implementation as much as algorithm; the transferable quantity is
scaling, and the log--log exponents against foreground pixels are
\RuntimeDnaiScalingExponent{}, \RuntimePlectaScalingExponent{} and at least
\RuntimeGraftScalingExponent{}. PLECTA grows fastest, but only GraFT is
heavy-tailed.

\subsection{Crossing resolution and crossing representation}
\label{sec:res-overlap}

Two distinct things are meant by an overlap-aware result, and the metric treats
them differently. The first is whether a crossing is \emph{resolved} correctly:
of the arms meeting at a junction, which continues into which. That is the
difficult decision, and it is what the common-fragment metric measures
(Section~\ref{sec:mm-metrics}), so every score in this paper rewards getting it
right. The last two rows of Table~\ref{tab:plecta-comparators} measure it directly:
over the \PlectaJunctionCount{} reference crossings of the degraded set, the
fraction whose incident arms a method partitions exactly as the reference does.
It orders the methods as the
grouping scores do, PLECTA's paired margins over the greedy baseline, GraFT and
DNAi being \PlectaJunctionGreedyDelta{} [\PlectaJunctionGreedyDeltaCILow{},
\PlectaJunctionGreedyDeltaCIHigh{}], \PlectaJunctionGraFTDelta{}
[\PlectaJunctionGraFTDeltaCILow{}, \PlectaJunctionGraFTDeltaCIHigh{}] and
\PlectaJunctionDNAiDelta{} [\PlectaJunctionDNAiDeltaCILow{},
\PlectaJunctionDNAiDeltaCIHigh{}]; clean axes order them the same way, PLECTA at
\PlectaJunctionCleanPlectaRate{}. Chance is high here, since leaving every arm
unpaired already resolves \PlectaJunctionChance{} of crossings, so the
chance-corrected resolution index accompanies the raw rate in the table and
never replaces it. The measure shares its assignment thresholds with the
common-fragment metric, so it is a sharper view of the same evidence rather
than independent confirmation of it.

The second is whether the emitted raster \emph{represents} a crossing pixel as
belonging to both filaments. That is a property of the output format, not of
the grouping decision, and the metric cannot see it. Flattening either method's layers to a single label image accordingly
changes \Fone{} by at most \PlectaOverlapFlattenCost{} for PLECTA and 0.002
for GraFT.

The representation is instead verifiable directly: on the
comparison set, reference instances share \PlectaOverlapReferenceLow{}\,\% of
their pixels with another instance at 20\% coverage, rising to
\PlectaOverlapReferenceHigh{}\,\% at 60\%. PLECTA emits
\PlectaOverlapExpressedLow{}--\PlectaOverlapExpressedHigh{}\,\% shared pixels;
GraFT, whose output is close to a hard partition, about
\PlectaOverlapGraFTHigh{}\,\%. Which pixels those are is less flattering:
PLECTA recovers \PlectaSharedRecall{} of the pixels the reference gives to more
than one instance, the only method here that recovers most of them, at a
precision of \PlectaSharedPrecision{}, marking
\PlectaSharedExcess{}--\PlectaSharedCleanExcess{} times more shared pixels than
the reference contains, because a chain is painted with every pixel of every
junction cluster it touches --- a property of the rendering, not of the
grouping. Neither that output nor GraFT's near-hard partition, a real
structural limitation, is anything the \Fone{} rewards or penalises;
Section~\ref{sec:limitations} takes up what that means for the comparison.

Figure~\ref{fig:examples} shows what the score corresponds to scene by scene,
taking at three coverage levels the scene whose \Fone{} is closest to the
median, so the rows are typical rather than selected. Its instances are drawn
at their fitted widths, because the comparison with the reference is only
legible once they are; the \Fone{} beside each row is the score of the
centreline grouping those widths are drawn onto.

\begin{figure}[htb]
    \centering
    \includegraphics[width=\linewidth]{figures/fig_plecta_examples.pdf}
    \caption{Reconstruction at three coverage levels, each the scene closest to
    the median \Fone{} of its stratum. Columns: the degraded input mask, the
    overlap-aware reference, and PLECTA's instances drawn at their fitted
    widths. What is evaluated in this paper is the grouping of centreline
    pixels the method produces from the mask alone, and the \Fone{} beside each
    row is that grouping's score; the width rendering shown here is a
    downstream step that redraws those instances and cannot move a pixel from
    one to another. Instance colours are arbitrary and not matched between
    columns.}
    \label{fig:examples}
\end{figure}

\subsection{Input-mask robustness}
\label{sec:res-mask}

On the separate \PlectaRobustnessSceneCount{}-scene controlled set,
mean \Fone{} was \PlectaRobustnessDegradedFOneMean{} from degraded axes and
\PlectaRobustnessCleanFOneMean{} from the corresponding clean axes. The paired
clean-minus-degraded change was \PlectaCleanMinusDegradedFOneMean{} (95\% CI
[\PlectaCleanMinusDegradedFOneCILow{},
\PlectaCleanMinusDegradedFOneCIHigh{}]). Modelled gaps and raster
irregularities therefore caused a small resolved loss under this generator.
Mask thickness costs more (Figure~\ref{fig:sensitivity}), which matters because
it is the upstream segmenter, not the method, that fixes that thickness. Each
width variant is scored against common fragments rebuilt from its own input
mask, so the question is how well the method groups the skeleton it is handed;
the decline with width is a thicker mask skeletonising into a messier graph,
not a change in the grouping problem.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\linewidth]{figures/fig_plecta_sensitivity.pdf}
    \caption{How PLECTA responds to the input mask it is handed, on
    development scenes. (a)~Nominal binary axis-mask width. (b)~The degraded
    1\,px axis against the generator's undegraded axis. Lines are means over the
    four development scenes at each coverage level; bands are the min--max
    range, a description of spread and not an interval. Exploratory, $n=4$
    scenes per point, no inference.}
    \label{fig:sensitivity}
\end{figure}

\subsection{Development ablations and downstream rendering}
\label{sec:res-ablation}

One-at-a-time ablations were evaluated only on the
\PlectaDevelopmentSceneCount{} development scenes
(Table~\ref{tab:plecta-ablation}).
The three largest losses relative to the evaluated configuration were, in
order, the junction chord-turn term
(\PlectaAblationTangentTermOnlyDeltaFOne{}), gap matching
(\PlectaAblationNoGapBridgingDeltaFOne{}) and connector-based junction-cluster
consolidation (\PlectaAblationNoCrossingMergingAtAllDeltaFOne{}); every other
component, chain-extended frames and the round schedule included, mattered
less. These are development values, not held-out effect estimates.

\begin{table}[htbp]
    \centering
    \caption{One-at-a-time component ablations on the
    \PlectaDevelopmentSceneCount{}-scene development set. Negative $\Delta F_1$
    values denote losses relative to the evaluated configuration.}
    \label{tab:plecta-ablation}
    \small
    \input{results/plecta_ablation_table}
\end{table}

The core output is a grouping, not a set of connected curves: accepted gap
links establish identity but are not painted, so on the development set
\PlectaCoreDisconnected{} of core instances arrive as more than one disjoint
pixel run and \PlectaCoreBranched{} contain a branch point. Any downstream step
that assumes one connected curve per instance must account for this. Default
Stage~4 ribbon rendering scored
development \Fone{}=\PlectaStageFourFOne{}, a change of
\PlectaStageFourDeltaFOne{}, and reduced those fractions to
\PlectaStageFourDisconnected{} and \PlectaStageFourBranched{}, so rendering buys
connectivity at a small cost in the identity score without changing chain
membership. Enabling the separate 0.6 silhouette-absorption rule reduced
\Fone{} further to \PlectaStageFourAbsorbFOne{}, and because absorption can
merge identities it remained disabled. None of these topology measurements
validates apparent width.

The negative control of Section~\ref{sec:res-factorial} holds scene by scene and
not only on average: in every one of the \PlectaFactorialGeometryCount{}
geometries, widening the bundles moves realised areal coverage by up to a factor
of \PlectaFactorialMaxCoverageRatio{} while \Fone{} moves by
\PlectaFactorialMaxFOneRange{}, which is small against the
\PlectaFactorialFOneSpread{} spread between geometries that is the scale it
should be read at. PLECTA's errors are also two-sided, where a fragment
\Fone{} can be reached one-sidedly: it sits on the precision--recall diagonal,
whereas the connected-component floor recovers \PlectaCCFloorRecall{} of
same-filament pairs at a precision of only \PlectaCCFloorPrecision{}.

\subsection{Two-field CNT SEM mask-source case}
\label{sec:res-real}
\label{sec:res-qualitative}

The two real SEM fields are the proof of concept for the whole route, and are
exploratory development cases. From the manual-derived axis, an oracle-input
condition, PLECTA reached \Fone{}=\PlectaBFiveEightOneZeroZeroManualFOne{} on
B58\_100 and \PlectaBFiveEightOneOneZeroManualFOne{} on B58\_110; the U-Net
condition, and the agreement of those axes with the manual skeleton, are in
Table~\ref{tab:plecta-real-masks}. The distance between the conditions is
the practical dependence on upstream mask quality. These values
are descriptive and do not estimate real-image generalisation, for the reasons
set out in Section~\ref{sec:limitations}.

\begin{table}[htbp]
    \centering
    \caption{Exploratory reconstruction from manual-derived and U-Net
    axes in two CNT SEM fields. Axis recall and precision compare the U-Net
    skeleton with the manual-derived skeleton using a symmetric
    \PlectaMaskQualityTolerance{}-px tolerance.}
    \label{tab:plecta-real-masks}
    \small
    \input{results/plecta_real_masks_table}
\end{table}

Those two agreement figures are, under their usual names, the completeness and
correctness of the tolerant completeness/correctness/quality triple standard for
thin structures; its third member, quality, is the tolerant Jaccard, and is also
GraFT's published Jaccard index in its intended reading, so it is what lets a
reader from that literature place these masks. At the same
\PlectaMaskQualityTolerance{}-px tolerance it is
\PlectaMaskQualityBFiveEightOneZeroZeroQualityThree{} on B58\_100 and
\PlectaMaskQualityBFiveEightOneOneZeroQualityThree{} on B58\_110. This measures
binary segmentation and not grouping: no reconstruction is opened to compute it,
and it characterises the input PLECTA is handed rather than anything PLECTA
does.

The tolerance is not a formality here, and reporting the triple without saying
so would mislead. Quality falls to
\PlectaMaskQualityBFiveEightOneZeroZeroQualityOne{} and
\PlectaMaskQualityBFiveEightOneOneZeroQualityOne{} at 1\,px, because the manual
annotation and the U-Net output are two independent readings of the same
filaments and disagree in placement almost everywhere. On the synthetic degraded
axis, where the evaluated mask is derived from the reference rather than read
from the image a second time, the same measure is nearly flat above 1\,px
(\PlectaMaskQualitySyntheticOne{} against
\PlectaMaskQualitySyntheticThree{} over
\PlectaMaskQualitySyntheticSceneCount{} scenes) and collapses to
\PlectaMaskQualitySyntheticZero{} at zero tolerance, where it reports raster
placement rather than geometry. The real masks are the looser input by a margin
this makes explicit, and that, rather than anything about the reconstruction, is
most of the distance between the two conditions of
Table~\ref{tab:plecta-real-masks}.

Four other published mask and instance measures were examined against these
data and not adopted, the evidence being recorded with the comparison study:
GraFT's Jaccard as published scores an unthinned prediction and so charges a
mask for its own thickness (\PlectaMaskQualityGraFTAsPublished{} where the
corrected form on the same masks is \PlectaMaskQualitySyntheticThree{}); its
false-flag ratio divides by the reference and is therefore unbounded above,
with no reading as a rate; its one-to-one filament-matching count saturates
because it scores existence rather than correctness, ranking PLECTA fourth of
five where every metric of Table~\ref{tab:plecta-comparators} ranks it first;
and DNAi's detection \Fone{} at an intersection-over-union threshold of 0.5
calls the undegraded mask harder than the degraded one, which is a statement
about instance length rather than about quality.
