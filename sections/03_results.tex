\section{Results}
\label{sec:results}

\subsection{Held-out synthetic reconstruction}
\label{sec:res-density}

PLECTA completed all \PlectaHeldoutSceneCount{} held-out scenes with
\PlectaHeldoutFailureCount{} failures. Mean common-fragment pairwise
\Fone{} was \PlectaHeldoutFOneMean{} (density-stratified 95\% bootstrap CI
[\PlectaHeldoutFOneCILow{}, \PlectaHeldoutFOneCIHigh{}]); mean precision,
recall, reference-instance recovery, and adjusted Rand index were
\PlectaHeldoutPrecisionMean{}, \PlectaHeldoutRecallMean{},
\PlectaHeldoutRecoveryMean{}, and \PlectaHeldoutARIMean{}, respectively
(Table~\ref{tab:plecta-heldout}). These scenes used unseen seeds but came from
the same procedural generator as development data, so the result measures
same-generator generalisation rather than distribution transfer.

\begin{table}[t]
    \centering
    \caption{Held-out reconstruction scores overall and by target areal
    coverage. VI denotes variation of information; lower VI is better, whereas
    higher values are better for the other metrics.}
    \label{tab:plecta-heldout}
    \small
    \input{results/plecta_heldout_table}
\end{table}

Performance decreased with scene density (Figure~\ref{fig:density}).
Mean \Fone{} fell from \PlectaDensityTwoZeroFOneMean{}
[\PlectaDensityTwoZeroFOneCILow{}, \PlectaDensityTwoZeroFOneCIHigh{}] at 20\% coverage
to \PlectaDensitySixZeroFOneMean{}
[\PlectaDensitySixZeroFOneCILow{}, \PlectaDensitySixZeroFOneCIHigh{}] at 60\% coverage;
the intermediate 30\% and 40\% means were \PlectaDensityThreeZeroFOneMean{} and
\PlectaDensityFourZeroFOneMean{}. Recorded per-scene runtime had mean
\PlectaHeldoutRuntimeMean{}~s, median \PlectaHeldoutRuntimeMedian{}~s,
90th percentile \PlectaHeldoutRuntimePNinety{}~s, and maximum
\PlectaHeldoutRuntimeMaximum{}~s; these timings are hardware-specific.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\linewidth]{figures/fig_plecta_density.pdf}
    \caption{PLECTA against target areal coverage on 50 paired development
    scenes, ten per coverage level, with each scene scored twice: once from the
    degraded 1\,px axis mask that the paper evaluates and once from the
    generator's undegraded axis. Lines are means of per-scene common-fragment
    $F_1$; the band is a 95\,\% percentile bootstrap confidence interval for
    the mean, resampled within each coverage stratum (20\,000 replicates, seed
    20260811). Degradation costs about 0.045 in $F_1$ at 20\,\% coverage and
    nothing measurable at 50--60\,\%, where the tangles rather than the gaps
    dominate. Target areal coverage is the generator setting used to dial in
    centreline density; it is a proxy for difficulty and not a cause of it,
    since holding the geometry fixed and varying only bundle width moves
    realised areal coverage by a factor of 2.49 while $F_1$ moves by 0.000
    (Figure~\ref{fig:performance}b).}
    \label{fig:density}
\end{figure}

\subsection{Which density axis matters}
\label{sec:res-factorial}

Areal coverage is the axis the scenes above are generated and stratified by,
but it is not obviously the axis that makes grouping hard. Coverage is the
fraction of the frame filled by the rendered bundles, so it rises both when
filaments are added and when the same filaments are simply drawn thicker. Only
the first changes the grouping problem.

A factorial separates the two. Twelve geometries---three centreline length
densities crossed with four seeds---were each rendered at bundle widths of 6,
11 and 16 px. Within one geometry the input axis mask and the overlap-aware
centreline reference are byte-identical across the three widths, verified by
SHA-256, while the union of the thick supports changes: areal coverage varies by
up to a factor of \PlectaFactorialMaxCoverageRatio{} within a single geometry.
Any movement in the score across those three renderings would mean something in
the method or the metric reads the silhouette.

Across all \PlectaFactorialGeometryCount{} geometries the within-geometry
\Fone{} range was \PlectaFactorialMaxFOneRange{}, that is, exactly zero in
\PlectaFactorialZeroRangeCount{} of \PlectaFactorialGeometryCount{}
(Table~\ref{tab:plecta-factorial}). Areal coverage therefore cannot causally
affect the grouping score; PLECTA reads the axis mask, and the metric scores
centreline fragments, so neither sees the width the bundles were drawn at.
Centreline length density does move the score: mean \Fone{} fell from
\PlectaFactorialLowFOne{} to \PlectaFactorialHighFOne{} as centreline coverage
rose from \PlectaFactorialLowLengthDensity{} to
\PlectaFactorialHighLengthDensity{}. The coverage bands of the three length
densities overlap---an areal coverage near 0.31 occurs at all three---so
coverage does not identify a difficulty level on its own.

\begin{table}[t]
    \centering
    \caption{Bundle width against centreline length density,
    \PlectaFactorialSceneCount{} development scenes. Within each row the three
    bundle widths share one byte-identical axis mask and one reference, so the
    spread in areal coverage carries no score difference. Length density is
    centreline pixels per image pixel.}
    \label{tab:plecta-factorial}
    \small
    \input{results/plecta_factorial_table}
\end{table}

Two consequences follow for the rest of this paper. Density strata reported by
target areal coverage should be read as a proxy for the length density that
accompanies them under this generator, not as a claim about coverage itself.
And a mask drawn at a different thickness, as an upstream segmenter may well
produce, does not by itself move the grouping score.

\subsection{Comparison with greedy continuation}
\label{sec:res-greedy}

The contribution claimed in Section~\ref{sec:intro} is that a junction is
better solved as one exact matching, with an explicit priced option to leave an
arm unmatched, than by pairing endpoints greedily. Greedy priority-ranked
pairing is the rule used by SIFNE, by GraFT's path cover, and by the continuity
baseline of Wegmayr et al.\ \citep{zhang2017sifne,osterlund2026graft,%
wegmayr2020microplastic}, so the claim is tested directly against it. The
comparator skeletonises the same mask, prunes spurs, splits the skeleton into
branches, and then greedily accepts the lowest-turning-angle pairing between
branch ends within a distance gate of 28~px and a turning-angle gate of
$20^\circ$. Both methods received identical masks and were scored through the
same common-fragment procedure on
\PlectaGreedySceneCount{} scenes generated after both configurations were
fixed.

PLECTA scored \PlectaGreedyPlectaFOne{} against
\PlectaGreedyBaselineFOne{}, a paired difference of
\PlectaGreedyDeltaFOne{} (95\% CI [\PlectaGreedyDeltaCILow{},
\PlectaGreedyDeltaCIHigh{}]), and was better in
\PlectaGreedyWinCount{} of \PlectaGreedySceneCount{} scenes
(Table~\ref{tab:plecta-greedy}). Adjusted Rand index and variation of
information rank the two methods the same way, so the result does not depend on
the in-house measure. The gap is widest in recall and in reference-instance
recovery, which is the expected signature: a greedy pass commits to the locally
straightest continuation and cannot revise it when that choice leaves a better
partner stranded, so single filaments are left divided. On clean axes the
difference was \PlectaGreedyCleanDeltaFOne{}
[\PlectaGreedyCleanDeltaCILow{}, \PlectaGreedyCleanDeltaCIHigh{}], so it is not
an artefact of mask degradation. Figure~\ref{fig:examples} shows the best,
median and worst scenes for PLECTA under that contrast.

\begin{table}[t]
    \centering
    \caption{PLECTA against a greedy minimum-turning-angle continuation
    baseline on \PlectaGreedySceneCount{} scenes with identical input masks and
    one scorer. Differences are paired, with density-stratified 95\% bootstrap
    intervals.}
    \label{tab:plecta-greedy}
    \small
    \input{results/plecta_greedy_table}
\end{table}

This is a comparison against one transparent rule, not against the
published methods that use it, and it is limited to this generator. Section
\ref{sec:limitations} states what a matched comparison against a released
implementation would still require.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\linewidth]{figures/fig_plecta_examples.pdf}
    \caption{Paired examples against a greedy minimum-turn continuation
    baseline, on the 50-scene comparison set. Rows are chosen by a stated rule
    rather than by inspection: the largest PLECTA advantage
    (\texttt{synth\_0003}, 60\,\% coverage, $\Delta F_1=+0.369$), the scene
    whose $\Delta F_1$ is closest to the median over all 50
    (\texttt{synth\_0006}, 30\,\% coverage, $+0.157$), and the largest
    comparator advantage (\texttt{synth\_0000}, 20\,\% coverage, $-0.036$).
    Columns are the degraded input mask, the overlap-aware reference, the
    comparator and PLECTA. Instance colours are arbitrary and carry identity
    only within a panel; they are not matched between columns. The reference
    column shows the generator's rendered ribbons while the two method columns
    show centreline instances, hence the difference in stroke width.}
    \label{fig:examples}
\end{figure}

\subsection{Input-mask robustness}
\label{sec:res-mask}

On the separate \PlectaRobustnessSceneCount{}-scene controlled set,
mean \Fone{} was \PlectaRobustnessDegradedFOneMean{} from degraded axes and
\PlectaRobustnessCleanFOneMean{} from the corresponding clean axes. The paired
clean-minus-degraded change was \PlectaCleanMinusDegradedFOneMean{} (95\% CI
[\PlectaCleanMinusDegradedFOneCILow{},
\PlectaCleanMinusDegradedFOneCIHigh{}]). Thus the modelled gaps and raster
irregularities caused a small resolved loss under this generator, without
testing robustness to a different image distribution. Widening the axis mask
costs more than degrading it (Figure~\ref{fig:sensitivity}): a thicker mask
skeletonises into a messier graph, which matters because an upstream segmenter
controls that thickness.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\linewidth]{figures/fig_plecta_sensitivity.pdf}
    \caption{How PLECTA responds to the input mask it is handed, on the
    development scenes. (a)~Nominal binary axis-mask width. (b)~The degraded
    1\,px axis that the paper evaluates against the generator's undegraded
    axis. Lines are means over the four development scenes at each coverage
    level and the band is the min--max range across those same four scenes ---
    a description of the spread, not an interval estimate. Exploratory, $n=4$
    scenes per point, no inference. Each variant is scored against common
    fragments rebuilt from its own input mask, so (a) asks how well the method
    groups the skeleton it is given rather than how many pixels it was given;
    the decline with width is the skeletonisation of a thicker mask producing
    a messier graph, not a change in the grouping problem.}
    \label{fig:sensitivity}
\end{figure}

\subsection{Development ablations and downstream rendering}
\label{sec:res-ablation}
\label{sec:res-factorial}

One-at-a-time ablations were evaluated only on the
\PlectaDevelopmentSceneCount{} development scenes
(Table~\ref{tab:plecta-ablation}).
Relative to the evaluated configuration, removing the junction chord-turn term
changed \Fone{} by \PlectaAblationTangentTermOnlyDeltaFOne{}, removing gap
matching by \PlectaAblationNoGapBridgingDeltaFOne{}, and disabling
connector-based junction-cluster consolidation by
\PlectaAblationNoCrossingMergingAtAllDeltaFOne{}. Removing chain-extended
frames and using a single round produced smaller changes of
\PlectaAblationNoChainExtendedFramesDeltaFOne{} and
\PlectaAblationSingleRoundDeltaFOne{}. These development results identify
chord-aware continuation, gap matching, and junction-topology preconditioning
as the largest tested contributors; they are not held-out effect estimates.

\begin{table}[t]
    \centering
    \caption{One-at-a-time component ablations on the 84-scene
    development set. Negative $\Delta F_1$ values denote losses relative to
    the evaluated configuration.}
    \label{tab:plecta-ablation}
    \small
    \input{results/plecta_ablation_table}
\end{table}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\linewidth]{figures/fig_plecta_performance.pdf}
    \caption{Three properties of the held-out result that a table of means
    cannot carry. (a)~All 128 held-out scenes, 32 per coverage stratum, with
    the stratum means printed above. The bands describe the spread across
    scenes and are not an interval for the mean; they overlap heavily, so one
    scene's score says little about its coverage. (b)~Twelve geometries from
    the development factorial, one line each. Along a line the centreline
    geometry is fixed and only bundle width changes: realised areal coverage
    moves by up to a factor of 2.49 while $F_1$ moves by 0.000. Difficulty
    follows centreline density, not the amount of ink on the page. (c)~The same
    128 scenes, one point each. PLECTA sits on the precision--recall diagonal;
    the one-instance-per-connected-component floor does not, recovering 0.85 of
    same-filament pairs at a precision of 0.047 because it merges every
    filament in a connected network into a single instance.}
    \label{fig:performance}
\end{figure}

The core output is a grouping, not a set of connected curves. Accepted gap
links establish identity but are not painted, so on the development set
\PlectaCoreDisconnected{} of core instances are delivered as more than one
disjoint pixel run and \PlectaCoreBranched{} contain a branch point. Readers
combining PLECTA with a downstream step that assumes one connected curve per
instance should account for this.

Default Stage~4 ribbon rendering achieved development \Fone{}=
\PlectaStageFourFOne{}, a change of \PlectaStageFourDeltaFOne{} from the core
centreline output, and reduced those fractions to
\PlectaStageFourDisconnected{} and \PlectaStageFourBranched{}. Rendering
therefore buys connectivity at a small cost in the identity score, without
changing chain membership.
Enabling the separate 0.6 silhouette-absorption rule reduced \Fone{} further
to \PlectaStageFourAbsorbFOne{}. Because absorption can merge identities, it
remained disabled and neither rendering condition contributed to the held-out
grouping result. These topology measurements do not validate apparent width.

\subsection{Two-field CNT SEM mask-source case}
\label{sec:res-real}
\label{sec:res-qualitative}

The two real SEM fields were exploratory development cases
(Table~\ref{tab:plecta-real-masks}). From the manual-derived axis, PLECTA
reached \Fone{}=\PlectaBFiveEightOneZeroZeroManualFOne{} on B58\_100 and
\PlectaBFiveEightOneOneZeroManualFOne{} on B58\_110. These are oracle-input conditions
because the axes were obtained from the manual instance annotations. With the
upstream U-Net axes, the corresponding values were
\PlectaBFiveEightOneZeroZeroUNetFOne{} and \PlectaBFiveEightOneOneZeroUNetFOne{}. The U-Net axis had
recall/precision \PlectaBFiveEightOneZeroZeroAxisRecall{}/\PlectaBFiveEightOneZeroZeroAxisPrecision{} on
B58\_100 and \PlectaBFiveEightOneOneZeroAxisRecall{}/\PlectaBFiveEightOneOneZeroAxisPrecision{} on
B58\_110.

\begin{table}[t]
    \centering
    \caption{Exploratory reconstruction from manual-derived and U-Net
    axes in two CNT SEM fields. Axis recall and precision compare the U-Net
    skeleton with the manual-derived skeleton using a symmetric 3-px tolerance.}
    \label{tab:plecta-real-masks}
    \small
    \input{results/plecta_real_masks_table}
\end{table}


B58\_100 was present in the segmenter's training data; only B58\_110 was
verified training-clean. Moreover, each input mask defines a different visible
common-fragment population, so the manual-derived and U-Net \Fone{} values are
descriptive rather than paired. With two development fields, these observations
show the practical dependence on upstream mask quality but do not estimate
real-image generalisation.
