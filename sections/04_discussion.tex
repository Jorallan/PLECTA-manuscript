%  One \section, three labels. sec:limitations is referenced from Section 2.3
%  and three times from Section 3, each time promising that this section
%  reports what the inter-annotator measurement shows, so the bounds below keep
%  those numbers rather than deferring them. sec:discussion and sec:conclusions
%  are kept as aliases so that any reference written against either still
%  resolves.
\section{Conclusions}
\label{sec:conclusions}
\label{sec:discussion}
\label{sec:limitations}

PLECTA reconstructs overlapping strand instances from a binary strand-axis mask
and nothing else. It solves every junction, of any degree, as a single exact
maximum-weight partial matching in which leaving an arm unmatched carries an
explicit price, so a strand is allowed to end rather than have a continuation
invented for it, and a crossing pixel is emitted to every strand that claims
it.

\begin{itemize}[leftmargin=1.5em,label=\textbullet,topsep=6pt,itemsep=2pt,parsep=0pt]
  \item Over \PlectaHeldoutSceneCount{} held-out synthetic scenes from unseen
        seeds it reaches mean pairwise
        \Fone{}=\PlectaHeldoutFOneMean{} [\PlectaHeldoutFOneCILow{},
        \PlectaHeldoutFOneCIHigh{}], falling from
        \PlectaDensityTwoZeroFOneMean{} to \PlectaDensitySixZeroFOneMean{} as
        areal coverage rises from 20 to 60\,\%.
  \item On identical masks it leads four published methods at every density,
        and the ordering holds on a competitor's own generator, run unmodified
        at its published settings.
  \item What carries the result is geometric: the largest ablation losses are
        the junction chord-turn term and gap bridging.
  \item A secondary stage reads the greyscale to order strands in depth. It is
        correct on \PlectaDepthDecidedLow{}--\PlectaDepthDecidedHigh{} of the
        crossings it commits to, against a chance level of
        \PlectaDepthChance{}, and abstains where the image does not separate
        the pair by more than the local noise.
  \item The grouping core uses no random number generator and no training data,
        and its configuration is published, so one mask yields one answer on
        every run. The demonstrated SEM route is not training-free: its mask
        comes from a trained CycleGAN and nnU-Net upstream.
\end{itemize}

\noindent\textbf{Bounds.}

\begin{itemize}[leftmargin=1.5em,label=\textbullet,topsep=6pt,itemsep=2pt,parsep=0pt]
  \item The held-out scenes share the generator family used in development, so
        their intervals are sampling variation within it and not transfer. What
        was checked on an external generator is the ranking, not the level.
  \item The comparison runs four methods on data none of them was built for. It
        supports a mechanical claim, that pairing endpoints greedily or
        committing with no option to decline loses identity in dense crossings,
        and does not establish that PLECTA is generally superior.
  \item The remaining sensitivity is the segmenter's. PLECTA can bridge a short
        omission, but it cannot infer a strand the mask omits, nor reliably
        reject a false axis introduced upstream.
  \item The real-material demonstration is a case study of
        \PlectaRealFieldCountLower{} CNT SEM fields and cannot estimate
        generalisation. Scored against the annotator whose skeleton it was
        given, PLECTA reaches
        \PlectaInterAnnotatorSameFLow{}--\PlectaInterAnnotatorSameFHigh{}, and
        across annotators
        \PlectaInterAnnotatorCrossFLow{}--\PlectaInterAnnotatorCrossFHigh{},
        while annotators~A and~B agree at
        \PlectaInterAnnotatorAgreeFLow{}--\PlectaInterAnnotatorAgreeFHigh{}
        with no method in the loop. The disagreement is systematic rather than
        error: B exports
        \PlectaInterAnnotatorObjectRatioLow{}--\PlectaInterAnnotatorObjectRatioHigh{}
        times as many objects as~A, shorter and far more fragmented, which is
        two answers to what an instance \emph{is}. A real-data score is
        therefore bounded by that disagreement rather than by the method, and
        identity through an occlusion is the part annotation cannot settle.
  \item Applicability follows the premise. The evidence is directional, so
        where direction does not survive a crossing, as in freely flexible
        chains, the method fails rather than degrades.
\end{itemize}

What would move any of this is independent real fields sealed before
configuration, validated physical widths, further released implementations run
in a matched comparison, and an external-generator benchmark with sealed seeds.
